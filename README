Authors: Adalberto Teixeira
	Matheus Batista Silva

To run the server backend: go to backend folder and run the service:
	node beCloserBackend.js

To run the application in index folder:
	python -m SimpleHTTPServer 
